export const HOST_DIR = './src/'

// !TODO: Change to explicit names (Source / Destination)
export const DOCKER_DIR = '/tmp/'
export const DOCKER_CODE_FILEPATH = `${DOCKER_DIR}code`
export const DOCKER_LOG_FILEPATH = `${DOCKER_DIR}output.log`
